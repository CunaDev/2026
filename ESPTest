-- ESP Public - V2
-- Fixed: tracer offset, box drift, team color, name color
-- Added: health bar position & thickness control
-- API: { Start(), Stop(), ESP = { ... } }

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local GuiService = game:GetService("GuiService")
local LocalPlayer = Players.LocalPlayer
local mouse = LocalPlayer:GetMouse()

local ESP = {
    State = false,
    Boxes = true,
    Health = true,
    Names = true,
    Distance = false,
    Tracers = true,
    TeamCheck = true,
    TeamColor = true,
    MaxDistance = 500,
    BoxColor = Color3.fromRGB(255, 60, 60),
    TracerColor = Color3.fromRGB(255, 60, 60),
    TracerThickness = 1,
    BoxThickness = 1,
    TracerOrigin = "Bottom",
    HealthBarPosition = "Top",    -- "Top" or "Bottom"
    HealthBarThickness = 4,
}

local ESP_UI = {}
local ESP_Connections = {}
local ESP_Render

-- Character parts used for stable 2D bounding box
local TRACK_PARTS = {
    "Head", "HumanoidRootPart", "Torso", "UpperTorso",
    "LowerTorso", "LeftFoot", "RightFoot", "LeftHand", "RightHand",
}

local function MakeUI()
    return {
        box = Drawing.new("Square"),
        boxOutline = Drawing.new("Square"),
        name = Drawing.new("Text"),
        dist = Drawing.new("Text"),
        healthBg = Drawing.new("Square"),
        healthBar = Drawing.new("Square"),
        tracer = Drawing.new("Line"),
        tracerOutline = Drawing.new("Line"),
    }
end

local function SetupUI(ui)
    ui.box.Filled = false
    ui.box.Thickness = 1
    ui.box.Visible = false

    ui.boxOutline.Filled = false
    ui.boxOutline.Color = Color3.fromRGB(0, 0, 0)
    ui.boxOutline.Thickness = 1
    ui.boxOutline.Visible = false

    ui.tracer.Thickness = 1
    ui.tracer.Visible = false

    ui.tracerOutline.Color = Color3.fromRGB(0, 0, 0)
    ui.tracerOutline.Thickness = 1
    ui.tracerOutline.Visible = false

    ui.healthBg.Filled = true
    ui.healthBg.Color = Color3.fromRGB(30, 30, 30)
    ui.healthBg.Visible = false

    ui.healthBar.Filled = true
    ui.healthBar.Visible = false

    ui.name.Center = true
    ui.name.Outline = true
    ui.name.Size = 13
    ui.name.Visible = false

    ui.dist.Center = true
    ui.dist.Outline = true
    ui.dist.Size = 12
    ui.dist.Visible = false
end

local function PartsBoundingBox2D(char, camera)
    local minX, maxX = math.huge, -math.huge
    local minY, maxY = math.huge, -math.huge
    local anyOnScreen = false

    for _, partName in ipairs(TRACK_PARTS) do
        local part = char:FindFirstChild(partName)
        if part and part:IsA("BasePart") then
            local pos, onScreen = camera:WorldToViewportPoint(part.Position)
            if onScreen then
                anyOnScreen = true
                if pos.X < minX then minX = pos.X end
                if pos.X > maxX then maxX = pos.X end
                if pos.Y < minY then minY = pos.Y end
                if pos.Y > maxY then maxY = pos.Y end
            end
        end
    end

    if not anyOnScreen then return nil end

    local pad = 4
    return {
        X = minX - pad,
        Y = minY - pad,
        Width = maxX - minX + pad * 2,
        Height = maxY - minY + pad * 2,
    }
end

local function GetTeamColor(plr)
    if ESP.TeamCheck then
        if plr.TeamColor == LocalPlayer.TeamColor then
            return Color3.fromRGB(0, 255, 0) -- same team = green
        else
            return Color3.fromRGB(255, 60, 60) -- enemy = red
        end
    end
    if ESP.TeamColor then
        return plr.TeamColor.Color
    end
    return ESP.BoxColor
end

local function Register(plr)
    if plr == LocalPlayer then return end
    local ui = MakeUI()
    SetupUI(ui)
    ESP_UI[plr] = ui
end

local function Unregister(plr)
    local ui = ESP_UI[plr]
    if ui then
        for _, d in pairs(ui) do
            pcall(d.Remove, d)
        end
        ESP_UI[plr] = nil
    end
end

local function Start()
    if ESP.State then return end
    ESP.State = true

    for _, plr in ipairs(Players:GetPlayers()) do
        Register(plr)
    end

    table.insert(ESP_Connections, Players.PlayerAdded:Connect(Register))
    table.insert(ESP_Connections, Players.PlayerRemoving:Connect(Unregister))

    ESP_Render = RunService.RenderStepped:Connect(function()
        local camera = workspace.CurrentCamera
        if not camera then return end
        local viewport = camera.ViewportSize
        local inset = GuiService:GetGuiInset()

        for plr, ui in pairs(ESP_UI) do
            local char = plr.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not char or not hum or hum.Health <= 0 then
                for _, d in pairs(ui) do d.Visible = false end
                continue
            end

            local root = char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart
            if not root then
                for _, d in pairs(ui) do d.Visible = false end
                continue
            end

            local rootPos, rootOnScreen = camera:WorldToViewportPoint(root.Position)
            local dist = (root.Position - camera.CFrame.Position).Magnitude
            if not rootOnScreen or dist > ESP.MaxDistance then
                for _, d in pairs(ui) do d.Visible = false end
                continue
            end

            -- Stable bounding box from tracked parts
            local bb = PartsBoundingBox2D(char, camera)
            if not bb then
                -- Fallback
                local h = math.clamp(1000 / dist * 80, 30, 320)
                local w = h * 0.6
                bb = {
                    X = rootPos.X - w / 2,
                    Y = rootPos.Y - h / 2,
                    Width = w,
                    Height = h,
                }
            end

            local color = plr.TeamColor and GetTeamColor(plr) or ESP.BoxColor
            local boxThick = ESP.BoxThickness
            local tracerThick = ESP.TracerThickness
            local hpThick = ESP.HealthBarThickness

            -- Box
            if ESP.Boxes then
                ui.boxOutline.Position = Vector2.new(bb.X - 1, bb.Y - 1)
                ui.boxOutline.Size = Vector2.new(bb.Width + 2, bb.Height + 2)
                ui.boxOutline.Thickness = boxThick + 2
                ui.boxOutline.Visible = true

                ui.box.Position = Vector2.new(bb.X, bb.Y)
                ui.box.Size = Vector2.new(bb.Width, bb.Height)
                ui.box.Thickness = boxThick
                ui.box.Color = color
                ui.box.Visible = true
            else
                ui.box.Visible = false
                ui.boxOutline.Visible = false
            end

            -- Health bar
            if ESP.Health then
                local maxhp = hum.MaxHealth
                local hpRatio = maxhp > 0 and math.clamp(hum.Health / maxhp, 0, 1) or 0
                local barWidth = bb.Width

                local hpY
                if ESP.HealthBarPosition == "Top" then
                    hpY = bb.Y - hpThick - 2
                else
                    hpY = bb.Y + bb.Height + 2
                end

                ui.healthBg.Position = Vector2.new(bb.X, hpY)
                ui.healthBg.Size = Vector2.new(barWidth, hpThick)
                ui.healthBg.Visible = true

                ui.healthBar.Position = Vector2.new(bb.X, hpY)
                ui.healthBar.Size = Vector2.new(barWidth * hpRatio, hpThick)
                ui.healthBar.Color = hpRatio > 0.5 and Color3.fromRGB(0, 240, 0)
                    or (hpRatio > 0.25 and Color3.fromRGB(255, 200, 0) or Color3.fromRGB(255, 40, 40))
                ui.healthBar.Visible = true
            else
                ui.healthBg.Visible = false
                ui.healthBar.Visible = false
            end

            -- Name
            if ESP.Names then
                ui.name.Text = plr.Name
                ui.name.Color = color
                ui.name.Position = Vector2.new(rootPos.X, bb.Y - 18)
                ui.name.Visible = true
            else
                ui.name.Visible = false
            end

            -- Distance
            if ESP.Distance then
                ui.dist.Text = math.floor(dist) .. "s"
                ui.dist.Position = Vector2.new(rootPos.X, bb.Y + bb.Height + 4)
                ui.dist.Visible = true
            else
                ui.dist.Visible = false
            end

            -- Tracer
            if ESP.Tracers then
                local origin
                if ESP.TracerOrigin == "Mouse" then
                    origin = Vector2.new(mouse.X, mouse.Y - inset.Y)
                elseif ESP.TracerOrigin == "Middle" then
                    origin = Vector2.new(viewport.X / 2, viewport.Y / 2)
                else
                    origin = Vector2.new(viewport.X / 2, viewport.Y)
                end

                local target = Vector2.new(rootPos.X, bb.Y + bb.Height / 2)

                ui.tracerOutline.From = origin
                ui.tracerOutline.To = target
                ui.tracerOutline.Thickness = tracerThick + 2
                ui.tracerOutline.Visible = true

                ui.tracer.From = origin
                ui.tracer.To = target
                ui.tracer.Thickness = tracerThick
                ui.tracer.Color = color
                ui.tracer.Visible = true
            else
                ui.tracer.Visible = false
                ui.tracerOutline.Visible = false
            end
        end
    end)
end

local function Stop()
    if not ESP.State then return end
    ESP.State = false
    for _, conn in ipairs(ESP_Connections) do
        pcall(conn.Disconnect, conn)
    end
    ESP_Connections = {}
    if ESP_Render then
        ESP_Render:Disconnect()
        ESP_Render = nil
    end
    for plr in pairs(ESP_UI) do
        Unregister(plr)
    end
end

return {
    Start = Start,
    Stop = Stop,
    ESP = ESP,
}
